{
  "name": "noisemaker-three",
  "version": "0.0.1",
  "description": "noisemaker shader platform for three.js — reuses the reference DSL compiler + pipeline verbatim, adds a three.js backend and integration surface",
  "type": "module",
  "license": "MIT",
  "author": "Noise Factor LLC <team@noisefactor.io>",
  "peerDependencies": {
    "three": ">=0.160.0"
  },
  "devDependencies": {
    "three": "^0.171.0",
    "esbuild": "^0.28.1",
    "@playwright/test": "^1.61.0",
    "pngjs": "^7.0.0",
    "eslint": "^10.5.0"
  },
  "scripts": {
    "vendor": "bash vendor/fetch.sh",
    "test": "node --test test/*.test.mjs",
    "lint": "eslint src/ test/",
    "build": "node tools/build.mjs",
    "parity": "bash parity/sweep-corpus.sh"
  }
}
