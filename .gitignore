node_modules/
dist/
parity/out/
parity/.venv/
*.log
.DS_Store

# Confidential reference — NEVER commit. The entire src/vendor/ tree is generated
# from the external reference by `npm run sync` (NM_REFERENCE_ROOT). It is the
# noisemaker reference platform, not work authored here; populate it locally.
src/vendor/
